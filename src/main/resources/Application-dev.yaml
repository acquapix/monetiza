spring:
  datasource:
    driverClassName: org.postgresql.Driver 
    url: jdbc:postgresql://localhost:5432/dbzinho
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    generate-dll: true
    show-sql: true

api:
  security:
    token:
      secret: my-secret-key